syntax = "proto3";

package node;

service NodeService {
    rpc StreamChunk(stream NodeStreamRequest) returns (NodeStreamResponse);
}

message NodeStreamRequest {
    string chunkId = 1;
    bytes chunk = 2;
}

message NodeStreamResponse{
    bool success = 1;
}